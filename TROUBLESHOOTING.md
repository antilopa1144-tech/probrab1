# 🔧 Решение проблем с интеграцией

## Проблема 1: "Калькуляторы не открываются"

### Симптомы:
- Клик по калькулятору → ничего не происходит
- Или открывается старый ProCalculatorScreen

### Решение:
Проверьте, что калькулятор зарегистрирован в навигации:

**Файл:** [calculator_navigation_helper.dart:62](lib/presentation/utils/calculator_navigation_helper.dart#L62)

```dart
// Новые калькуляторы с iOS-дизайном из HTML
if (definition.id == 'paint_universal' || definition.id == 'paint') {
  Navigator.of(context).push(
    ModernPageTransitions.scale(
      const PaintScreen(),
    ),
  );
  return;
}
```

### Как добавить свой калькулятор:
1. Узнайте ID калькулятора:
   ```bash
   grep "id:" lib/domain/calculators/your_calculator_v2.dart
   ```

2. Добавьте в `calculator_navigation_helper.dart`:
   ```dart
   if (definition.id == 'your_calculator_id') {
     Navigator.of(context).push(
       ModernPageTransitions.scale(
         const YourCalculatorScreen(),
       ),
     );
     return;
   }
   ```

---

## Проблема 2: "Половина калькуляторов не видна в категориях"

### Симптомы:
- Категория "Все" показывает много калькуляторов
- Категория "Стены" / "Пол" показывает мало или ноль

### Решение:
Добавлена автоматическая категоризация в [modern_calculator_catalog_screen.dart:98](lib/presentation/views/calculator/modern_calculator_catalog_screen.dart#L98)

Метод `_getCategoriesForCalculator()` автоматически определяет категории по `subCategory`.

### Правила автоматического определения:

| Категория | Ключевые слова в subCategory |
|-----------|------------------------------|
| **Стены** | `wall`, `paint`, `plaster`, `putty`, `gkl`, `interior` |
| **Пол** | `floor`, `screed`, `laminate`, `tile`, `linoleum`, `parquet` |
| **Отделка** | `paint`, `tile`, `finish`, `wallpaper`, `ceiling` |
| **Дерево** | `wood`, `parquet` |

### Как добавить калькулятор в категорию вручную:

В [modern_calculator_catalog_screen.dart:31](lib/presentation/views/calculator/modern_calculator_catalog_screen.dart#L31) добавьте в `toolsData`:

```dart
'your_calculator_id': {
  'icon': Icons.your_icon,
  'color': const Color(0xFF3B82F6),
  'bg': const Color(0xFFDBEAFE),
  'bgDark': const Color(0xFF1E3A8A),
  'cat': ['walls', 'floor'], // Ваши категории
},
```

---

## Проблема 3: "Локализация не работает"

### Симптомы:
- Видны ключи вместо текста: `calculator.paint.title`
- Пустые названия калькуляторов

### Решение:

1. **Проверьте наличие перевода** в [assets/lang/ru.json](assets/lang/ru.json):
   ```json
   {
     "calculator": {
       "paint": {
         "title": "Универсальная покраска",
         "description": "Расчёт краски для стен и потолка"
       }
     }
   }
   ```

2. **Проверьте ключ в калькуляторе:**
   ```dart
   titleKey: calculatorTitleKey('paint'), // создаст 'calculator.paint.title'
   ```

3. **Проверьте, что JSON загружается:**
   ```bash
   flutter clean
   flutter pub get
   ```

### Как добавить перевод для нового калькулятора:

1. Откройте `assets/lang/ru.json`
2. Добавьте секцию:
   ```json
   "calculator": {
     "your_id": {
       "title": "Название калькулятора",
       "description": "Описание калькулятора"
     }
   }
   ```
3. Повторите для других языков (`en.json`, `kk.json`, и т.д.)

---

## Проблема 4: "Темная тема не переключается"

### Симптомы:
- Клик на кнопку солнце/луна → ничего не происходит

### Решение:
Проверьте, что используется правильный метод:

**Файл:** [modern_calculator_catalog_screen.dart:190](lib/presentation/views/calculator/modern_calculator_catalog_screen.dart#L190)

```dart
ref.read(settingsProvider.notifier).updateDarkMode(!isDark);
```

НЕ используйте `toggleDarkMode()` - этого метода нет!

---

## Проблема 5: "Главный экран не изменился"

### Симптомы:
- Запустили приложение, но видите старый экран
- Нет нового Dashboard

### Решение:

1. **Проверьте замену в main_shell.dart:**

   **Файл:** [main_shell.dart:91](lib/presentation/app/main_shell.dart#L91)

   ```dart
   _buildTabNavigator(
     index: MainShell.homeTabIndex,
     child: const ModernCalculatorCatalogScreen(), // ✅ Должно быть это
   ),
   ```

   НЕ должно быть `NewHomeScreen`!

2. **Сделайте Hot Restart:**
   - Нажмите `Shift+R` в терминале Flutter
   - Или остановите и запустите заново

3. **Очистите build:**
   ```bash
   flutter clean
   flutter pub get
   flutter run
   ```

---

## Проблема 6: "Windows не компилируется (CMake Error)"

### Симптомы:
```
CMake Error at CMakeLists.txt:3 (project):
  No CMAKE_CXX_COMPILER could be found.
```

### Решение:

Это не влияет на код! Используйте эмулятор или другую платформу:

```bash
# Android
flutter run -d android

# iOS (на Mac)
flutter run -d ios

# Web
flutter run -d chrome
```

Или установите Visual Studio Build Tools для Windows.

---

## Проблема 7: "Hot Reload не применяет изменения"

### Симптомы:
- Изменили код
- Нажали `r` (hot reload)
- Ничего не изменилось

### Решение:

1. **Используйте Hot Restart вместо Hot Reload:**
   - Нажмите `R` (заглавная) вместо `r`
   - Или `Shift+R` в некоторых терминалах

2. **Полный перезапуск:**
   ```bash
   # Остановите
   flutter run --stop

   # Запустите заново
   flutter run
   ```

---

## Общие советы:

### Проверка изменений:
```bash
# Анализ кода
flutter analyze

# Запуск тестов
flutter test

# Очистка и перезапуск
flutter clean && flutter pub get && flutter run
```

### Логирование для отладки:

Добавьте в код:
```dart
print('Calculator ID: ${calc.id}');
print('Categories: ${_getCategoriesForCalculator(calc)}');
```

Смотрите логи в консоли Flutter.

---

## Быстрая проверка интеграции:

### ✅ Checklist:

- [ ] Главный экран заменен на `ModernCalculatorCatalogScreen`
- [ ] Навигация настроена в `calculator_navigation_helper.dart`
- [ ] Переводы есть в `assets/lang/ru.json`
- [ ] Категории работают (автоматически или в `toolsData`)
- [ ] PaintScreen открывается при клике
- [ ] WoodScreen создан и доступен
- [ ] Темная тема переключается кнопкой
- [ ] Поиск фильтрует калькуляторы

### 🧪 Тестирование:

1. Запустите приложение
2. Проверьте главный экран → должен быть новый Dashboard
3. Кликните на категорию "Стены" → должны появиться соответствующие калькуляторы
4. Найдите "Краска" и кликните → должен открыться PaintScreen
5. Нажмите кнопку темы → должна переключиться тема

---

## Нужна помощь?

Создайте issue с:
1. Описанием проблемы
2. Скриншотом
3. Логами из консоли
4. ID калькулятора, который не работает

Удачи! 🚀
