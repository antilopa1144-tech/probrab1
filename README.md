# 🏗️ Прораб AI

**Профессиональный калькулятор строительных материалов** для Android и iOS.

<p align="center">
  <img src="assets/icons/app_icon.png" width="120" alt="Прораб AI Logo">
</p>

## 📋 О проекте

**Прораб AI** — это мобильное приложение для расчёта строительных материалов с учётом актуальных цен по регионам России. Приложение создано для строителей, прорабов и людей, делающих ремонт своими руками.

### ✨ Ключевые возможности

- 🧮 **55+ калькуляторов** для всех видов работ:
  - Фундамент (ленточный, плитный, свайный)
  - Стены (покраска, обои, декоративная штукатурка, плитка)
  - Полы (ламинат, плитка, стяжка, тёплый пол)
  - Потолки (натяжные, ГКЛ, реечные)
  - Кровля (металлочерепица, мягкая кровля)
  - Наружная отделка (сайдинг, фасадные панели)
  - Инженерные работы (электрика, сантехника, отопление)

- 💰 **Региональные цены** для 5 регионов РФ:
  - Москва
  - Санкт-Петербург
  - Екатеринбург
  - Краснодар
  - Регионы РФ (усреднённые)

- 🌍 **Многоязычность** — поддержка 7 языков:
  - 🇷🇺 Русский
  - 🇬🇧 English
  - 🇰🇿 Қазақша
  - 🇰🇬 Кыргызча
  - 🇹🇯 Тоҷикӣ
  - 🇹🇲 Türkmençe
  - 🇺🇿 O'zbek

- 📱 **Современный UI/UX**:
  - Material You 3 дизайн
  - Тёмная и светлая темы
  - Динамический акцентный цвет
  - Плавные анимации

- 💾 **Оффлайн режим**:
  - Локальная база данных Isar
  - История расчётов
  - Сохранение проектов

---

## 🚀 Установка и запуск

### Требования

- Flutter SDK >= 3.10.0
- Dart >= 3.0.0
- Android Studio / Xcode
- iOS 12+ / Android 6.0+

### Шаг 1: Клонирование

```bash
git clone https://github.com/your-username/probrab_ai.git
cd probrab_ai
```

### Шаг 2: Установка зависимостей

```bash
flutter pub get
```

### Шаг 3: Генерация кода для Isar

```bash
flutter pub run build_runner build --delete-conflicting-outputs
```

### Шаг 4: Запуск

```bash
# Debug режим
flutter run

# Release режим
flutter run --release

# Для конкретной платформы
flutter run -d android
flutter run -d ios
```

### Сборка APK/IPA

```bash
# Android APK
flutter build apk --release

# Android App Bundle
flutter build appbundle --release

# iOS (требуется macOS)
flutter build ipa --release
```

---

## 🏛️ Архитектура

Проект построен на принципах **Clean Architecture** с использованием **Riverpod** для управления состоянием.

```
lib/
├── core/                    # Инфраструктурный слой
│   ├── animations/          # Анимации переходов
│   ├── errors/              # Обработка ошибок
│   ├── localization/        # Локализация (7 языков)
│   ├── widgets/             # Переиспользуемые виджеты
│   ├── constants.dart       # Константы приложения
│   └── theme.dart           # Material You 3 тема
│
├── data/                    # Слой данных
│   ├── datasources/         # Источники данных (JSON, API)
│   ├── models/              # Модели данных (Isar)
│   └── repositories/        # Реализации репозиториев
│
├── domain/                  # Бизнес-логика
│   ├── calculators/         # Определения калькуляторов
│   ├── entities/            # Доменные сущности
│   └── usecases/            # UseCase-ы калькуляторов (55+)
│
├── presentation/            # UI слой
│   ├── app/                 # Главные экраны
│   ├── components/          # UI компоненты
│   ├── providers/           # Riverpod провайдеры
│   ├── services/            # Сервисы (PDF, экспорт)
│   └── views/               # Экраны по категориям
│
└── main.dart                # Точка входа
```

### Паттерны и принципы

| Паттерн | Применение |
|---------|------------|
| **Clean Architecture** | Разделение на слои: data, domain, presentation |
| **UseCase** | Бизнес-логика калькуляторов изолирована |
| **Repository** | Абстракция доступа к данным |
| **Provider (Riverpod)** | Управление состоянием |
| **Strategy** | Калькуляторы реализуют общий интерфейс |

### Диаграмма зависимостей

```
┌─────────────────────────────────────────────────────────┐
│                    Presentation                          │
│  ┌─────────┐  ┌──────────┐  ┌─────────────────────────┐ │
│  │ Screens │  │ Providers│  │      Components         │ │
│  └────┬────┘  └────┬─────┘  └───────────────────────┘  │
│       │            │                                    │
└───────┼────────────┼────────────────────────────────────┘
        │            │
        ▼            ▼
┌─────────────────────────────────────────────────────────┐
│                      Domain                              │
│  ┌──────────────┐  ┌─────────────┐  ┌────────────────┐  │
│  │  UseCases    │  │  Entities   │  │  Definitions   │  │
│  │ (55+ calc)   │  │             │  │ (калькуляторы) │  │
│  └──────┬───────┘  └─────────────┘  └────────────────┘  │
│         │                                               │
└─────────┼───────────────────────────────────────────────┘
          │
          ▼
┌─────────────────────────────────────────────────────────┐
│                       Data                               │
│  ┌───────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │ Repositories  │  │    Models    │  │  DataSources │  │
│  │ (Isar, JSON)  │  │    (Isar)    │  │    (JSON)    │  │
│  └───────────────┘  └──────────────┘  └──────────────┘  │
└─────────────────────────────────────────────────────────┘
```

---

## 📁 Структура файлов

### Калькуляторы (`lib/domain/usecases/`)

Каждый калькулятор реализует интерфейс `CalculatorUseCase`:

```dart
abstract class CalculatorUseCase {
  CalculatorResult call(
    Map<String, double> inputs,
    List<PriceItem> priceList,
  );
}
```

### Конфигурация калькуляторов (`lib/domain/calculators/definitions.dart`)

Определения полей, валидации и советов:

```dart
CalculatorDefinition(
  id: 'floors_tile',
  titleKey: 'calculator.tile',
  category: 'Внутренняя отделка',
  subCategory: 'Полы',
  fields: [
    InputFieldDefinition(
      key: 'area',
      labelKey: 'input.area',
      minValue: 0.5,
      maxValue: 500.0,
    ),
    // ...
  ],
  tips: const [
    'Используйте крестики для равномерного шва.',
    // ...
  ],
  useCase: CalculateTile(),
)
```

### Региональные цены (`assets/json/`)

```
assets/json/
├── prices_moscow.json
├── prices_spb.json
├── prices_ekaterinburg.json
├── prices_krasnodar.json
└── prices_regions.json
```

### Локализация (`assets/lang/`)

```
assets/lang/
├── ru.json    # Русский
├── en.json    # English
├── kk.json    # Казахский
├── ky.json    # Киргизский
├── tg.json    # Таджикский
├── tk.json    # Туркменский
└── uz.json    # Узбекский
```

---

## 🧪 Тестирование

### Запуск всех тестов

```bash
flutter test
```

### Запуск тестов калькуляторов

```bash
flutter test test/domain/usecases/
```

### Тесты с покрытием

```bash
flutter test --coverage
genhtml coverage/lcov.info -o coverage/html
```

---

## 📦 Зависимости

### Основные

| Пакет | Версия | Назначение |
|-------|--------|------------|
| `flutter_riverpod` | ^2.6.1 | State management |
| `isar` | ^3.1.0+1 | Локальная БД |
| `shared_preferences` | ^2.5.3 | Настройки |
| `intl` | ^0.20.2 | Локализация |
| `pdf` | ^3.11.3 | Генерация PDF |
| `share_plus` | ^12.0.1 | Шаринг |

### Firebase

| Пакет | Версия | Назначение |
|-------|--------|------------|
| `firebase_core` | ^3.11.0 | Firebase SDK |
| `firebase_analytics` | ^11.6.0 | Аналитика |
| `firebase_crashlytics` | ^4.3.0 | Отчёты об ошибках |

### Разработка

| Пакет | Версия | Назначение |
|-------|--------|------------|
| `flutter_lints` | ^6.0.0 | Линтинг |
| `isar_generator` | ^3.1.0+1 | Генерация кода |
| `build_runner` | ^2.4.6 | Код-генерация |

---

## 🎨 Темы и дизайн

### Material You 3

Приложение использует Material You 3 с динамическим акцентным цветом:

```dart
// lib/core/theme.dart
static ThemeData darkTheme(Color accent) {
  final colorScheme = ColorScheme.fromSeed(
    seedColor: accent,
    brightness: Brightness.dark,
  );
  // ...
}
```

### Цветовая палитра

- **Тёмная тема**: `#0F0F0F` (фон), динамический акцент
- **Светлая тема**: `#FAFAFA` (фон), динамический акцент
- **Акцент по умолчанию**: `#FFC107` (янтарный)

---

## 📄 Лицензия

MIT License — свободное использование с указанием авторства.

---

## 🤝 Контрибьютинг

1. Форкните репозиторий
2. Создайте ветку: `git checkout -b feature/new-calculator`
3. Сделайте коммит: `git commit -m 'Add new calculator'`
4. Пуш: `git push origin feature/new-calculator`
5. Создайте Pull Request

### Добавление нового калькулятора

1. Создайте UseCase в `lib/domain/usecases/`
2. Добавьте определение в `lib/domain/calculators/definitions.dart`
3. Добавьте переводы в `assets/lang/*.json`
4. Напишите тесты в `test/domain/usecases/`

---

## 📞 Поддержка

- 📧 Email: support@probrab-ai.ru
- 🐛 Issues: [GitHub Issues](https://github.com/your-username/probrab_ai/issues)
- 💬 Telegram: [@probrab_ai](https://t.me/probrab_ai)

---

<p align="center">
  Made with ❤️ for строителей России и СНГ
</p>
