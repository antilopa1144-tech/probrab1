# Руководство по рефакторингу definitions.dart

## Проблема

`lib/domain/calculators/definitions.dart` содержит **3048 строк** и 64+ импорта калькуляторов. Это затрудняет:
- Навигацию по коду
- Поддержку и добавление новых калькуляторов
- Code review
- Время компиляции

## Решение: Модульная архитектура

### Новая структура

```
lib/domain/calculators/
├── registry/
│   ├── calculator_definitions_base.dart   ✅ Создан (базовые классы)
│   ├── all_calculators.dart               ✅ Создан (агрегатор)
│   ├── foundation_calculators.dart        ⏳ TODO
│   ├── wall_calculators.dart              ⏳ TODO
│   ├── floor_calculators.dart             ⏳ TODO
│   ├── ceiling_calculators.dart           ⏳ TODO
│   ├── partition_calculators.dart         ⏳ TODO
│   ├── insulation_calculators.dart        ⏳ TODO
│   ├── exterior_calculators.dart          ⏳ TODO
│   ├── roofing_calculators.dart           ⏳ TODO
│   ├── engineering_calculators.dart       ⏳ TODO
│   ├── bathroom_calculators.dart          ⏳ TODO
│   ├── mixture_calculators.dart           ⏳ TODO
│   ├── windows_doors_calculators.dart     ⏳ TODO
│   ├── sound_insulation_calculators.dart  ⏳ TODO
│   └── structure_calculators.dart         ⏳ TODO
├── definitions.dart                       🔴 Устарел (3048 строк)
└── ... другие файлы V2 ...
```

### Преимущества новой структуры

1. **Читаемость**: каждый файл ~200-300 строк вместо 3000+
2. **Производительность**: Dart компилирует модули параллельно
3. **Git**: изменения в одной категории не конфликтуют с другими
4. **Lazy loading**: в будущем можно подгружать категории по требованию

## План миграции (поэтапный)

### Фаза 1: Подготовка (✅ Выполнено)

- [x] Создан `calculator_definitions_base.dart` с базовыми классами
- [x] Создан `all_calculators.dart` с обратной совместимостью
- [x] Старый код продолжает работать через импорт

### Фаза 2: Создание файлов категорий (🔜 Следующий шаг)

Для каждой категории создайте файл по шаблону:

```dart
// foundation_calculators.dart
import 'calculator_definitions_base.dart';
import '../../usecases/calculate_strip_foundation.dart';
import '../../usecases/calculate_slab.dart';

/// Калькуляторы фундамента.
final List<CalculatorDefinition> foundationCalculators = [
  CalculatorDefinition(
    id: 'calculator.stripTitle',
    titleKey: 'calculator.stripTitle',
    category: 'Фундамент',
    // ... остальные поля
  ),
  // ... другие калькуляторы
];
```

**Порядок создания** (от наиболее используемых):
1. `foundation_calculators.dart` (2 калькулятора)
2. `wall_calculators.dart` (~10 калькуляторов)
3. `floor_calculators.dart` (~8 калькуляторов)
4. ... остальные

### Фаза 3: Миграция импортов

Замените во всех файлах:

```dart
// ❌ Старый способ
import 'package:probrab_ai/domain/calculators/definitions.dart';

// ✅ Новый способ
import 'package:probrab_ai/domain/calculators/registry/all_calculators.dart';
```

### Фаза 4: Удаление старого кода

После полной миграции:
1. Удалите `definitions.dart`
2. Уберите временные импорты из `all_calculators.dart`
3. Запустите тесты: `flutter test`

## Автоматизация (опционально)

Создайте скрипт для автоматического извлечения категорий:

```bash
# Извлечь категорию "Фундамент" из definitions.dart
dart run scripts/extract_category.dart foundation
```

## Оценка трудозатрат

- Создание одного файла категории: **10-15 минут**
- Всего категорий: **14**
- Общее время: **~3 часа**
- Миграция импортов: **30 минут** (Find & Replace)
- Тестирование: **30 минут**

**Итого: ~4 часа** для полного рефакторинга

## Текущий статус

🟢 **Инфраструктура готова** — можно начинать миграцию
🟡 **Обратная совместимость** — старый код работает
🔴 **Требуется действие** — создать файлы категорий

---

## Альтернативный подход: Постепенная миграция

Можно не удалять `definitions.dart`, а использовать гибридный подход:

1. Новые калькуляторы добавляйте в модульные файлы
2. Старые оставьте в `definitions.dart`
3. Мигрируйте по одной категории в неделю

Это снижает риски и позволяет делать рефакторинг постепенно.
